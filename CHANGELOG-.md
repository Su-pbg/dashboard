# CHANGELOG 추가분 (통합)

앞서 조각으로 드린 `CHANGELOG-추가분-2 ~ 7`은 **버리고 이 파일만** `Su-pbg/dashboard/CHANGELOG.md` 맨 위에 붙여넣으세요.

---

## 2026-07-30 ~ 08-03

### 잘못된 숫자를 고침

**상품 목록이 상품 상세로 분류되고 있었음**
`detail` 판정 규칙에 `/product/` 가 들어 있어 `/product/list.html` 까지 상세로 잡혔고, `list` 규칙은 뒤에 있어 도달하지 못했다. 목록을 먼저 판정하도록 순서를 바꾸고 조건을 좁혔다.
→ 상품조회 전환율 **88.5% → 39.8%**, 상품 목록 조회수 거의 0 → 48만.

**퍼널 단계마다 세는 단위가 달랐음**
방문=세션, 상품조회=페이지뷰, 장바구니=이벤트 횟수 가 섞여 통과율이 단위가 다른 값끼리의 나눗셈이었다(새로고침이 +1, 상품 5개 담으면 +5). 5단계 전부 "그 단계에 도달한 세션 수"로 통일. 전환율 KPI 3종과 일별 추이도 동일 기준.
→ 장바구니·구매 수치가 작아짐. 부정확했던 것이 정확해진 결과.

**할인 필드 오타로 예치금이 누락되고 있었음**
`credit_spent_amount` 로 적혀 있었으나 실제 응답은 `credits_spent_amount`(복수형).

**`items[]` 안의 할인을 훑지 않았음**
실측 주문으로 확인: `39,000 − 32,370 = 6,630` 인데 `coupon_discount_price 3,900 + items[0].additional_discount_price 2,730 = 6,630`. `기타` 로 흡수됐던 2,730이 상품 추가할인이었다.
→ 해당 주문의 `기타` 가 2,730 → **0**.
※ `items[].coupon_discount_price` 는 주문 레벨과 같은 금액이 중복 표기된 것이라 제외(이중 계상 방지).

**월 단위 지표가 진행 중인 달을 포함하고 있었음**
카드 이름은 `최근 완료월` 인데 8월 3일에 8월(3일치)이 들어가 있었다.
→ 전월 대비 **-94.6% → +34.7%**, 추이 마지막 점이 0으로 곤두박질치던 문제와 평균선 왜곡 해소. 매출·객단가·MAU 세 차트 모두 적용.

**MAU 가 유일하게 제외 필터를 안 받고 있었음**
`buildMonthlyUsers` 에 파라미터 자체가 없어 정크 캠페인이 그대로 들어갔다(평소 5~12만이 34만으로 표시).

**없는 지표를 표시하고 있었음**
장바구니가 필수 경로인데 `바로구매 추정(= 주문서 세션 − 장바구니 세션)` 을 '장바구니 안 거침' 으로 표시했다. 존재할 수 없는 경로다. 실제 의미는 ① 세션 이월 ② 상세→주문서 직행으로 페이지뷰 누락 ③ GA4 누락 — 추적 점검 지표다. `장바구니 페이지 미기록` 으로 이름을 바꾸고 원인을 명시.

**카테고리 집계가 13개월간 비어 있었음**
`/admin/products` 목록 응답에는 `category` 필드가 아예 없다(`embed=categories` 도 무시됨). 단건 조회 방식으로 재작성.

**카테고리 대표를 '가장 작은 번호'로 뽑아 무의미해졌음**
`228 = SHOP`(전체 상품 공용)이 최소 번호라 거래액 95%가 여기로 몰렸다. `301 = 개인결제창` 은 분류도 아니다. 번호 크기로는 판정할 수 없다(`240 패브릭` 은 작은 번호인데 실제 분류).
→ 분류 트리를 런타임에 받아 **아트(608)/라이프(609) 두 축**으로 재설계. 양쪽에 속한 상품은 중복 계상하고 그 사실을 화면에 표시. 축을 펼치면 상품명별 내역이 나온다.

### 상품명이 채워지지 않던 문제

두 단계로 원인이 드러났다.

1. `slice(0, 15)` 로 후보를 앞 15개만 잘랐다. Set 앞부분을 매번 같이 자르니 16번째 이후 상품은 조회를 시도조차 하지 않아 캐시에도 영구히 안 들어갔다.
2. 목록(배치) 조회가 일부 상품(미진열·판매중지 등)을 반환하지 않는데, 응답에 없는 번호를 `null` 로 확정 저장해 재조회를 막았다. 재조회 낭비를 없애려던 처리가 원인이 되어 있었다.

→ 배치 조회(100개/1회) 후 **빠진 것만 단건으로 보완**하고, 단건까지 실패한 것만 `null` 로 확정. 캐시 키 버전을 올려(`productNames:v2`, `slotIdentityNames:v3`) 굳어버린 `null` 을 폐기. TTL 1일 → **30일**. KV 키를 상품마다 두던 것을 번들 1개로 묶어 읽기 60회 → 1회.

### 성능

**워커 CPU 한도 초과로 구좌 탭이 죽던 문제**
`buildSlots`/`buildSlotPerf` 만 4개 차원(pagePath × pageReferrer × sourceMedium × country)을 써서 211일 구간에서 행이 7만 개가 됐고, 행마다 문자열을 만들어 비교하니 약 88ms — 무료 플랜 한도(요청당 10ms)의 8배였다. 워커가 죽으면 CORS 헤더를 붙일 기회조차 없어 브라우저에는 CORS 에러로 보였다.
→ `sourceMedium`·`country` 는 거르는 용도로만 썼으므로 제외 조건을 GA4 `dimensionFilter` 로 넘기고 두 차원을 응답에서 제거. 차원 4→2개, 추정 행 7만→3천, CPU 88ms→0.1ms.

**같은 요청을 두 번 보내고 있었음**
`loadHeroCharts` 와 `loadTrend` 가 `tab=daily` 를 완전히 동일한 주소로 각각 조회했다. 비교를 켜면 4번(캐시를 무시하는 새로고침 버튼에서는 GA4 조회도 4번). 한 조회 안에서 같은 URL 이면 결과를 공유하는 `wfetch` 도입, 워커 호출 13곳 전환.

**GA4 조회를 하나씩 기다리고 있었음**
`buildData` 가 전/후 기간과 각종 지표를 순차로 await 했다. 서로 의존하지 않는데도.
→ `oneRange` 내부(이벤트·페이지), 전/후 기간, `standardMetrics` 3개, `sessionsInRange` 2개를 모두 병렬로. **await 지점 12곳 → 5곳.** GA4 응답 400ms 가정 시 6초 → 0.8초 수준.

**서브요청 한도 관리**
상품명 보완 로직을 넣으니 최악의 경우 45/50이 되어 한도에 근접했다(넘으면 위와 같은 CORS 증상). 예산을 조정해 **32/50** 으로 여유 확보.

### 데이터 유실을 막음

`build_revenue_daily.py` 가 매일 최근 90일을 교체할 때 `newMembers` 만 챙기고 `withdrawnMembers`·`totalMembers` 를 버렸다. 그대로 두면 다음 새벽 실행에서 병합해둔 회원 데이터가 통째로 사라진다. 이어받을 필드를 `CARRY_OVER` 목록으로 관리하도록 수정. 백필 스크립트도 동일.

### 신규가입 수집 방식 교체

기존: `/admin/dashboard` 의 '오늘 기준' 누적값을 자정 직전에 스냅샷. 실행이 밀리면 그날 값이 영구 손실(실제로 8/1~8/2 누락).
→ **가입 완료 페이지(`join_result`) 도달 세션 수**로 교체. GA4 는 날짜 지정 조회가 자유로워 언제든 다시 읽을 수 있고 과거 구간도 채워진다. 7월 1,156명 = CSV 실제 1,184명의 **97.6%** 포착.
조회수가 아니라 세션 수를 쓰는 이유: 완료 페이지 새로고침은 조회수를 늘리지만 가입은 한 번이다.
※ `snapshot_new_members.py` 와 그 스케줄은 이제 불필요.

### 회원 데이터

cafe24 회원 목록 API 는 전부 막혀 있음을 확인(아래 '확인된 API 제약'). 관리자 CSV 병합으로 처리.
`newMembers` / `withdrawnMembers` / `totalMembers` 3필드를 577일(2025-01-01 ~ 2026-07-31) 전 구간에 채웠다. 검증: 마지막 날 회원 수 37,697명이 CSV 총계와 일치, `회원수 증분 = 신규 − 탈퇴` 불일치 0건(576개 구간).
`유지` 섹션에 총 회원 수·순증·이탈률 KPI 와 추이 차트 추가. 탈퇴 데이터가 없는 기간에는 순증·이탈률 카드를 표시하지 않고, 데이터가 뒤처지면 기준일을 안내한다.
※ `totalMembers` 는 시점 잔액(stock)이라 합산하면 안 된다. `STOCK_FIELDS` 로 버킷 합산에서 제외(30일 구간에서 30배가 되는 것을 방지).

### 필터

**한국 면제를 제거.** 예전에는 해외 트래픽만 검사해서 한국발 정크 캠페인(하루 5만 세션·체류 2초·구매 0)이 그대로 통과했다. 판별 기준을 국적에서 행동으로 전환. 오분류 방어는 '집단 평균' 구조가 담당한다 — 진짜 고객이 섞인 조합은 평균 체류가 5초 밑으로 내려갈 수 없다.

**세션 하한을 기간에 비례화**: `max(5, 조회일수 × 2)`. 고정 5는 긴 기간에서 사실상 전수검사가 되어 기준이 흔들렸다. 바닥값 5는 하루 조회에서 표본 1~2개의 우연으로 채널이 통째로 제외되는 것을 막는다. **상수 2·5 는 임의값**이며 `?tab=debugfilter` 로 실데이터를 보고 조정할 수 있다.

**캠페인 축 신설.** 정크가 여러 소스로 흩어져도 캠페인명으로 묶어서 잡는다. 캠페인명이 없는 자연/직접 유입은 대상이 아니라 일반 고객 오분류 위험이 없다. 적용 규모: GA4 쿼리 31곳.

**체크박스 이름 정정.** `성과 없는 트래픽 제외` → **`봇·어뷰징 트래픽 제외`**. 실제 판정 기준은 "채널 평균 체류 5초 미만"이므로, 성과가 나쁘지만 사람이 실제로 온 트래픽(예: `ig/paid-social` 48,430세션·전환율 0.52%)은 걸러지지 않는다. 이름이 하는 일과 다르면 오해를 부른다.

**의도적 예외 1곳**: `국가별 유입` 은 자동제외를 걸지 않는다. '무엇을 걸러야 하는지 찾는' 용도라 걸러버리면 판별이 불가능하다. 수동 제외(L포인트 등)만 적용.

**필터 투명성.** 자동 제외된 캠페인이 조용히 사라지지 않도록 응답에 `excludedCampaigns` 를 담고 UTM 표 상단에 안내를 표시.

### 화면

**목표 추적으로 전환.** 매주 병목을 새로 찾는 방식은 무엇을 추적하는지가 흐려진다. 단기 목표 2~3개를 정해 같은 숫자만 확인하는 구조로 바꿨다. 목표값은 코드가 아니라 **`goals.json`** 에 있어 숫자만 고치면 되고, 파일이 없으면 블록이 나타나지 않는다.
진행률은 '목표까지 남은 거리'가 아니라 **'시작점에서 얼마나 움직였나'**(`(현재−시작)/(목표−시작)`)로 계산한다. 절대 위치만 보면 시작이 낮았을 때 늘 초라해 보인다. 후퇴하면 빨간 막대, 달성하면 초록 + `달성` 배지.
맨 위에 **거래액(GMV)·실매출**과 그 차이(할인·환불)를 둔다. 비율만 있으면 규모가 잡히지 않는다.

**북마크로 설정 고정.** 기간·비교방식·필터·탭이 주소에 담긴다. **날짜가 아니라 프리셋 번호를 저장**하는 것이 핵심 — 날짜를 저장하면 북마크가 며칠 뒤 낡은 기간을 보여준다. `?p=1` 이면 열 때마다 '최근 7일'이 최신으로 재계산된다.

**긴 기간 자동 묶기.** 차트 그림영역이 758px 고정이라 365일을 일별로 그리면 슬롯이 2px가 되어 막대가 겹쳤다. `~62일 일별 / ~180일 주별 / 181일~ 월별`. 숫자 필드를 합산하므로 호출부의 비율·객단가 수식은 자동으로 가중평균이 되어 한 줄도 고치지 않았다(3개 구간에서 직접 계산값과 일치 확인). 주별 라벨은 달력상 월요일이 아니라 '선택 기간에 실제로 포함된 첫날'로 표기 — 그러지 않으면 경계의 첫 칸에 범위 밖 날짜가 찍힌다.

**퍼널을 전환율·이탈률 중심으로.** 통과율과 이탈률을 큰 숫자로 올리고 세션 수는 보조로 내렸다. 증감 색을 지표별로 맞춤(통과율은 오르면 초록, 이탈률은 오르면 빨강 — 이전엔 같은 규칙이라 이탈률 상승이 좋아 보였다).

**정보가 잘리던 곳을 접기로.** `.slotbar-row` 이름 칸이 좁은데 `nowrap`+ellipsis 가 걸려 병목 정보가 잘렸다. 칸을 넓히는 것은 임시방편이라 `자세히 ▾` 토글을 도입. 펼친 상태에서는 `white-space:normal` 로 풀어야 전문이 보인다. 토글 버튼은 `.slotbar-name` **밖에** 두어야 한다 — 안에 두면 이름이 긴 세그먼트에서 버튼까지 잘린다.

**의미 없는 막대 제거.** 하위 상품은 세션 수가 비슷해 막대 길이가 다 같았다.
※ 구좌 행의 비교 막대는 지시 없이 제거했던 것이라 **복원**했다.

**KPI 카드 한 줄 최대 4개.** `repeat(4, ...)` 로 고정했더니 카드가 3개인 섹션은 네 번째 칸이 빈 채로 남았다. `auto-fit + minmax(250px)` 로 바꿔 카드 수에 맞춰 채워진다.

**요일별 평균.** `viewBox 920px` SVG 가 520px 칸에 들어가 56%로 축소되어 눈금·라벨을 읽을 수 없었다. 데이터 점이 7개뿐이므로 전체 폭 가로 막대 목록으로 교체. 같은 함정을 반복하지 않도록 `.chart-grid`·`.mini-chart` CSS 삭제.

**이중 축 제거.** 매출(0~6억)과 환불(0~1억)을 다른 축에 그려 환불 선이 매출 선 위로 올라가는 착시를 만들었다(실제로는 8배 작은데 더 커 보임). 같은 축으로 통일.

**설명문 축약.** 27개 1,900자 → 15개 279자. 해보면 아는 것("마우스를 올리면 수치가 보입니다")은 지우고, 모르면 오해하는 것만 남겼다.

**중복 제거.** 탭마다 반복되던 기간 라벨 5곳 삭제(상단 날짜 입력과 비교 방식 드롭다운이 같은 정보를 이미 보여준다).

**명칭 정정.** `정가 주문액(GMV)` → `거래액(GMV)`, 개요의 `매출총액`(실제로는 net) → `실매출`. 개요 수익 KPI 에 거래액·실매출 추가(맨 앞). `신규 가입`·`가입 전환율` 은 **획득** 그룹 — 가입은 사이트에 발을 들이는 행동이고, 활성화는 들어와서 상품을 보고 담는 단계다.

**아이콘·기호.** 새로고침의 `↻`(U+21BB)는 Pretendard 에 없어 대체 폰트로 그려져 베이스라인이 어긋났다. 인라인 SVG 로 교체하고 정사각형 아이콘 버튼으로. 컬러 이모지 `⚠️` 제거(환경마다 모양이 다름). 진단 항목명은 대괄호 표기(`[가장 큰 병목]`).

### 디자인 토큰

카드 테두리를 전면 제거하고 회색 배경 위 흰 카드로 면을 구분. 모노스페이스(IBM Plex Mono) 폐기 — `body` 에 `font-variant-numeric: tabular-nums` 를 걸어 Pretendard 로도 숫자 자리가 맞는다. Pretendard 를 동적 서브셋 + variable 로 교체(쓰는 글자만 내려받음). 4배수 여백 토큰(`--s1`~`--s8`), 반경·색 토큰 정리. 키보드 포커스 링, `prefers-reduced-motion` 존중.
타이포는 한 차례 키운 뒤 다시 한 단계 축소(데이터가 촘촘한 대시보드에는 과했음). 최종 스케일 28/24/19/16/15/14/13/12/11.

### 확인된 API 제약 — 같은 시도를 반복하지 말 것

**회원 목록 조회 불가**
- `GET /admin/customers` → **422**. `cellphone` 또는 `member_id` 가 필수라 가입일 범위 조회가 불가능하다(개별 조회 전용).
- `/admin/customers/count`, `/admin/customers/withdrawal`, `/admin/withdrawnmembers`, `/admin/customersgroups` → 전부 **404**
- `/admin/customersprivacy` → **403** insufficient_scope
- 통계 API(`ca-api.cafe24data.com`) members/* → 전부 404
- API 버전 지정 시 **400** — 이 앱은 **2026-03-01 고정**
⇒ 신규가입은 GA4 페이지뷰로 대체 완료. **탈퇴는 여전히 CSV 수동 추출만 가능.**

**주문**
- 목록 조회 `embed=coupons` 에는 `coupons` 키가 오지 않는다(71개 키에 없음). 쿠폰명·쿠폰별 금액은 **단건 조회**에만 있다.
- `/admin/coupons`(쿠폰 마스터) → **403** insufficient_scope. 다만 주문에 쿠폰명이 직접 오므로 불필요.

**상품**
- 목록 조회 응답에 `category` 필드 없음. `embed=categories` 도 무시됨. 카테고리는 단건 조회에만 존재.
- `/admin/products/{no}/categories` → 404
- 목록 조회는 일부 상품(미진열·판매중지 등)을 반환하지 않는다 → 배치 후 단건 보완 필요.
- `product_no` 를 쉼표로 이어 **한 번에 100개**까지 조회 가능.

**탈퇴 CSV 특성**
- 탈퇴회원 CSV 에는 `회원 가입일`·`휴대폰번호`가 비어 있다(탈퇴 시 개인정보 삭제). 잔존 회원 CSV 와 아이디 교집합 **0건** — 두 집합이 완전히 분리되어 어떤 키로도 조인할 수 없다.
- 따라서 탈퇴자의 가입일을 알 수 없어 `newMembers` 보정은 불가능하다. 다만 탈퇴는 19개월간 188명(전체의 0.5%)이고, 스냅샷과 CSV 를 비교했을 때 겹치는 93일 중 90일이 완전히 일치하고 3일만 1명 차이였다. 보정하지 않아도 무방.

### 남은 작업

- **과거 구간 백필** — 쿠폰·아트/라이프 분류가 최근 3개월만 채워져 있다. `overwrite` 로 분기별 실행(한 번에 전 구간은 2시간 타임아웃에 걸림). 회원 필드는 이제 보존된다.
- **탈퇴 자동 수집** — 가입처럼 탈퇴 완료 페이지 방문으로 셀 수 있는지 확인(`?tab=debugpages&q=withdraw`).
- **필터 상수 확정** — `?tab=debugfilter` 로 실데이터를 보고 세션 하한을 조정.
- **주문서 이탈 원인** — 비회원 구매가 막혀 있어 주문서에서 가입을 강제한다. 신규 전환율(0.34%)이 재방문(0.82%)의 41%뿐인 것과 직결된다. 회원 정책 판단 근거로 재구매 분석 스크립트(`analyze_repurchase.py`)를 준비해 두었다.
