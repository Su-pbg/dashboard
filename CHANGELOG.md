# PBG 대시보드 변경 이력

최신 변경이 맨 위에 옵니다. 각 항목에 **배포 대상**이 표시돼 있으니, 올릴 위치를 확인하세요.

| 파일 | 배포 위치 |
|---|---|
| `index.html`, `CHANGELOG.md`, `revenue-daily.json` | `Su-pbg/dashboard` (GitHub Pages) |
| `worker.js` | Cloudflare Worker `proud-sea-35f9` |
| `build_revenue_daily.py`, `snapshot_new_members.py`, `*.yml` | `Su-pbg/slackbot` |

---

## 2026-09-10

### [버그] 새 정렬 구좌가 글로벌네비 실적에 잘못 합산됨
**배포:** `worker.js` (Cloudflare) + `index.html` (GitHub Pages)

- **증상:** ART 메인에 새로 붙은 `조회수 TOP` / `인기 상품` 구좌가 구좌 성과 표에 없음
- **원인:** 두 구좌의 링크가 `cate_no=608&sort_method=8` / `&sort_method=6`. 그런데 `buildSlotPerf`는 목적지를 `cate_no=(\d+)` 로만 묶어서 **둘 다 cate_no=608 로 합쳐졌고**, 608은 이미 글로벌네비 '아트' 구좌로 등록돼 있음
  → 새 구좌가 빠지는 정도가 아니라 그 트래픽이 **글로벌네비 '아트' 실적으로 잘못 더해짐** (9일 기준 239세션이 118세션짜리 자리에 얹혀 있었음)
- **조치**
  - SLOT_MAP 에 `sort` 필드 지원. `cate_no + sort_method` 조합을 별도 구좌로 집계 (`byCS`)
  - 같은 cate 에 sort 변형이 있으면 부모 구좌는 **sort_method 없는 유입만** 집계 (`byCsortless`) → 이중 계상 제거
  - ART `{o:59 조회수 TOP, o:60 인기 상품}`, LIFE `{o:61 인기 상품, o:62 신상품}` 추가
  - cafe24 목록 정렬 코드: **5=신상품순, 6=인기순, 8=조회수순**

### 미등록 구좌 자동 발견 — 프론트에서 수행
**배포:** `index.html` (워커 수정 없음)

- **계기:** 구좌가 늘 때마다 SLOT_MAP 을 손으로 못 따라가서 조용히 새고 있었음. 실제로 9/1~9/9 기준 SLOT_MAP 에 없는 목적지가 **6개**(`994`·`948`·`1034`·`1017`·`629`·`637`) — `994`는 ART 4위 유입처(153세션)인데 표에 아예 없었음
- **워커를 계속 고치지 않아도 되도록 감지를 프론트로 옮김.** 등록된 자리는 `slotperf` 응답 각 행의 `cate`/`sort`에서, 실제 유입처는 `slots` 응답 목적지 목록에서 얻어 비교 → SLOT_MAP 을 프론트에 복제할 필요 없음
- 5세션 이상인 것만 상위 10개 노출. 상품 상세(`product_no=` 포함)는 구좌가 아니므로 제외
- 각 행이 실제 페이지 링크라서 눌러보면 어느 자리인지 바로 확인됨
- ART에 `sort_method=5`(신상품) 구좌가 있으면 이것도 자동으로 잡힘
- **참고:** 반대로 등록돼 있는데 9일간 유입 0인 구좌가 ART 23개·LIFE 22개 (수공당 작가별 807~812, 사이즈별 734~739, 테마별 740~744, 금액별 723~727 등). 칩 UI 개편으로 사라진 것으로 보이며 정리 필요

---

## 2026-09-08

### 구매하기 버튼 분리(2026-09-07) 반영
**배포:** `index.html`, `scripts/fetch-data.mjs` (GitHub Pages)

- **변경 내용:** 기존 [구매하기] = 장바구니 담기 → 장바구니 → 주문서. 2026-09-07부터 [구매하기]는 장바구니를 거치지 않고 **주문서로 직행**, [장바구니 담기]는 별도 버튼으로 분리.
- `BUY_SPLIT_DATE = '2026-09-07'` 상수 추가 + `buySplitPhase()` / `hasDirectBuy()` / `buySplitNotice()` 헬퍼
- **구매 전환 퍼널:** 장바구니 담기를 선형 체인에서 제외하고 **선택 경로 · 장바구니** 섹션으로 분리
  - 이유: 담지 않고 바로 산 세션이 전부 "장바구니에서 이탈"로 잡혀 이탈률이 부풀려짐
  - 체인: 방문 → 상품조회 → 결제(주문서) → 구매
  - 장바구니는 `상품조회 → 담기율`로 별도 표시
- **가장 큰 병목(`fillBottleneck`)** 계산에서도 장바구니 단계 제외
- **장바구니 분석 · 구매 경로:** `directEstimate`(주문서 세션 − 장바구니 세션)의 해석을 뒤집음
  - 분리 전: `장바구니 페이지 미기록` (추적 점검용 참고치)
  - 분리 후: **`바로구매 (주문서 직행)`** — 실제 구매 경로. 단 세션 이월·GA4 누락이 섞이므로 추세로 볼 것
  - `장바구니 이탈률` / `담기→구매 전환율` 설명 문구도 분리 후 기준으로 수정
- **기간이 2026-09-07을 걸치면** 퍼널·장바구니 섹션 상단에 주황색 경고 배너, 추이 그래프의 `장바구니 담기`에 "버튼 분리로 단절" 배지 표시
- **목표(`cartRate`) 안내:** 장바구니 전환율은 구조적으로 낮아지므로 목표치·기준선 재설정 필요
- `scripts/fetch-data.mjs`: funnel의 `장바구니 담기` 단계에 `branch` 플래그 추가 (기간 종료일이 분리일 이후면 `true`)
- **미적용:** 워커(`worker.js`)의 `cartAnalysis` 계산식은 그대로. 해석만 대시보드에서 바뀜

### 데이터 건강 검진 배너 추가
**배포:** `index.html` (GitHub Pages)

- **계기:** 2026-08-01부터 `newMembers`/`totalMembers`가 38일간 통째로 null이었는데 대시보드가 아무 경고 없이 빈칸으로 그렸다. 월간 분석을 돌릴 때까지 아무도 몰랐음
- 화면 최상단(`#dataHealth`)에 수집 중단 경고를 띄움. 검사 항목:
  1. `revenue-daily.json` 마지막 갱신이 36시간 초과 → 매출 수집 중단
  2. 최근 7일 `newMembers` / `totalMembers` 가 전부 null → 회원 스냅샷 중단 (며칠째인지 + 마지막 정상 기록일 표시)
  3. 최근 30일 카테고리 항목의 80% 초과가 '미분류' → 카테고리 이름 매핑 실패
  4. 최근 30일 카테고리 합계가 GMV의 70% 미만 → 매핑 커버리지 부족
- 상수 `HEALTH_STALE_HOURS`(36) / `HEALTH_LOOKBACK`(7) 로 임계치 조정 가능

### [정정] `newMembers` 공백은 고장이 아니라 폐기된 필드
**배포:** `index.html`

- 2026-08-03에 cafe24 스냅샷 방식(`/admin/dashboard`의 '오늘 기준' 값을 자정 직전 캡처)을 폐기하고,
  GA4 가입완료 페이지(`join_result`/`join_end`) 기준 **`신규 가입` KPI로 이미 대체**했다
- 워커는 8/3부터 `신규 가입`·`가입 전환율`을 `group:'acquisition'`으로 내려주고 있고,
  `byGroup('acquisition')`이 그대로 받아 **획득 섹션에 정상 렌더 중** (확인 완료)
- 따라서 `newMembers`가 08-01부터 null인 것은 **정상 상태**. 검진 배너에서 이 항목 검사 제거
  (멀쩡한 걸 고장으로 오해하게 만드는 오탐이었음)

### `totalMembers` 과거 시점 되짚기 (`tab=membercount`)
**배포:** `worker.js` (Cloudflare) + `index.html` (GitHub Pages)

- **문제:** `totalMembers`가 2026-07-31 마지막, 08-01~09-07 전부 null. 누적 회원 수는 cafe24만 아는 값이라 GA4로 대체 불가. `/admin/customers/count`는 "지금" 값만 줘서 과거 복원이 안 되는 줄 알았음
- **해결:** count 엔드포인트에 **가입일 상한**(`created_end_date` 등)을 걸면 "그 날짜까지 가입한 회원 수" = 그 시점 누적 회원 수를 얻을 수 있음. 워커가 이미 `getCafe24AccessToken()`으로 토큰을 쥐고 있어 새 시크릿·새 파이프라인 없이 붙임
- **워커 `tab=membercount`**
  - 가입일 상한 파라미터명을 **실제로 때려보고 판별** (과거 날짜로 값이 줄어드는지 확인 → 카페24가 모르는 파라미터를 조용히 무시하는 함정 방어). 판별 결과는 KV 30일 캐시
  - 기간이 길면 최대 31개 포인트로 균등 샘플링 (무료 플랜 서브요청 한도 50 대비, 실측 23회)
  - **앵커 검증:** 마지막 실측일(2026-07-31 = 37,697명)을 같은 방식으로 계산해 오차율(`anchor.diffPct`)을 함께 반환. 오차 1% 초과면 `reliable:false`
- **`index.html`**
  - `fillMissingTotalMembers()`가 공백 구간을 워커 값으로 메움. 채운 행은 `totalMembersEstimated:true`로 실측과 구분
  - `reliable:false`면 **채우지 않고** 사유를 화면에 표시 — 추정치를 조용히 정답처럼 넣지 않는다(이번 공백을 38일간 못 알아챈 원인이 그것)
  - 채운 경우에도 "N일치는 추정값 · 오차 X%"를 유지 섹션에 명시
  - 검진 배너는 `totalMembersEstimated`를 **수집된 것으로 치지 않음** → 추정으로 메워도 원천 수집 중단 경고는 유지
- **한계:** 그 뒤 탈퇴한 회원은 현재 목록에 없어 과거로 갈수록 실제보다 낮게 나옴. 근본 해결은 원천 수집 복구
- **미해결:** `withdrawnMembers`(탈퇴 수)는 여전히 공백 → 순증·이탈률은 이 기간 계산 불가

---

## 2026-07-30

### 슬랙봇 종료에 따른 시크릿 정리
**배포:** `*.yml` (slackbot)

- 슬랙 매출봇(`main.yml`) 사용 중단 → 스케줄 제거(수동 실행만 유지)
- 워크플로우 5개의 시크릿 참조를 `secrets.DASH_*` → `secrets.*`로 변경
  - `update-revenue.yml`, `update-new-members.yml`, `backfill-revenue.yml`, `backfill-traffic.yml`, `update-traffic-daily.yml`
- **이유:** `DASH_` 접두사 시크릿이 레포에 등록돼 있지 않아 워크플로우가 인증 실패로 중단됨
  (로그: `[인증 실패] 다음 환경변수(Secrets)가 비어 있습니다: ['GIST_ID', 'CLIENT_ID', 'CLIENT_SECRET']`)
- 원래는 슬랙봇과 토큰 충돌을 피하려고 분리했으나, 슬랙봇을 끄면서 분리 이유가 없어짐
- **남은 정리 대상(미적용):** 안 쓰는 시크릿 `SLACK_WEBHOOK_URL`, `PLAYAUTO_API_KEY`, `PLAYAUTO_SOLUTION_CODE`, `REFRESH_TOKEN`

### [버그] 신규 가입자 수가 계속 5명으로 기록됨
**배포:** `snapshot_new_members.py`, `update-new-members.yml` (slackbot)

- **증상:** 실제 45명인 날이 5명으로 기록. 7/27·28·29가 **모두 정확히 5**
- **원인:** cafe24 대시보드 API(`/admin/dashboard`)의 `new_members_count`는 **"오늘" 값만** 주고 날짜 지정이 불가.
  그런데 워크플로우가 **새벽 05:05**에 실행돼서 *자정~새벽 5시 가입자(≈5명)* 를 읽고 그것을 **어제 값으로 기록**
- **조치:** 회원 목록 API(`/admin/customers`)를 **가입일 범위로 조회해 날짜별로 직접 집계**
  - 날짜 지정이 되므로 실행 시각과 무관하게 정확
  - **최근 14일을 매번 재집계** → 잘못 기록된 과거 값 자동 교정
  - cafe24 파라미터명이 몰마다 달라 후보 3종을 순차 시도, 실패 시 응답의 가입일로 **로컬 재검증**
  - 집계 실패 시 기존 값을 건드리지 않고 중단(잘못된 값 덮어쓰기 방지)
- 실행 시각 05:05 → **05:20** (매출 스크립트와 같은 파일 동시 쓰기 방지)

### 성능 최적화 (index.html)
**배포:** `index.html`

- `revenue-daily.json` fetch가 **6곳 → 1곳**(`ensureRevenue()`)으로 통합.
  진행 중인 요청을 공유해서 **동시에 여러 곳이 불러도 요청 1번**만 발생
- `revenueRangeNet/Count/NewMembers` 3개 함수 → **`revenueRangeSum()` 하나**로 통합
  (같은 394일치 데이터를 3번 순회하던 것을 1번으로)
- 개요 탭 순차 `await` → **병렬 실행**
  - 객단가·신규가입자: 선택/비교 기간을 `Promise.all`로 동시 계산
  - 성장지표 + 유저세그먼트: `Promise.allSettled`로 동시 로드
- **죽은 CSS 8줄 제거** — `table.slot` 계열(본문에 `<table>`이 남아있지 않음), 미사용 `.seg`

### 퍼널 분석 확장
**배포:** `worker.js`, `index.html`

- **이탈 인원·이탈률** 표시 — 통과율(%)만으로는 개선 우선순위를 못 정해서 추가
  (비율이 낮아도 인원이 적으면 기회가 작고, 그 반대도 성립)
- **개선 기회 순위** — 이탈 **인원** 많은 순 정렬 + "10%만 살리면 +N명" 환산
- **세그먼트별 퍼널** (`tab=segfunnel`) — 신규/재방문·디바이스·유입채널별로 CVR 비교,
  각 세그먼트의 **최대 병목 구간**을 자동 표시
- **장바구니 분석 + 구매 경로** (`tab=cart`)
  - GA4에 `begin_checkout`이 없고 `remove_from_cart`가 있어 이벤트+페이지를 섞어 구성
  - "스스로 뺀 비율"(remove_from_cart) = 단순 이탈과 다른 **명시적 포기 신호**
  - 바로구매는 `주문서 세션 − 장바구니 세션`으로 **추정**(GA4 표준 리포트로 교집합 계산 불가 → 화면에 '추정' 명시)

### 매출 지표 확장
**배포:** `worker.js`, `index.html`, `build_revenue_daily.py`

- **할인 종류별** — 쿠폰/회원등급/적립금/예치금/배송비/세트·묶음/상품할인
  - cafe24 필드명이 몰마다 달라 **후보 키를 여러 개** 두고 있는 것만 합산
  - 분류 못 한 금액은 **「기타」로 흡수** → 종류별 합계가 총 할인액과 **항상 일치**
- **카테고리별 거래액·건수** — 상품→카테고리 매핑을 상품 API로 조회해 집계
  (거래액·판매개수·개당금액을 함께 표시해 "비싼 게 조금" vs "싼 게 많이" 구분)
- **할인액/할인율 KPI** 추가. 정의: `할인액 = 정가(GMV) − (실매출 + 환불)`
  - 최근 8개월 검증 결과 음수 발생 0건 → 정의 타당
- KPI를 **돈 흐름 순서**로 재배치: 정가 → 할인액 → 할인율 → 환불액 → 환불율 → 실매출 → 건수 → 객단가
- 할인·환불은 **낮을수록 좋은 지표**라 증감 색상을 반대로 처리(화살표 방향은 실제 그대로)

### [버그] 매출 탭 전체가 로드 실패
**배포:** `index.html`

- **증상:** "매출 데이터를 불러오지 못했습니다" (데이터 파일은 정상, 394일치 존재)
- **원인:** `drawRevenueTrend` 재작성 시 **범위를 잘못 잡아 `renderRevenue` 함수 전체가 삭제됨**
- **조치:** 함수 복원 + 할인 지표 반영

### 차트 개선
**배포:** `index.html`

- **호버 툴팁** 추가 — 브라우저 기본 `<title>` 대신 커스텀 툴팁.
  차트 어디에 올려도 가장 가까운 날짜 값 표시, 비교선이 있으면 선택/비교 동시 표시, 세로 커서선 동기화
- **평균선 라벨을 격자 영역 밖**(오른쪽 여백)으로 이동 — 선·점에 가려지는 문제 해결
  (`padR`을 104로 확보. 좌표 검증: 격자 끝 816 < 라벨 822)
- **차트 높이 240px 통일** — `compareLineChart`만 240, 나머지 220이라 나란히 놓으면 크기가 달라 보였음
- **막대그래프 비교 지원**(`verticalBarChart`) — 선택/비교를 **그룹 막대**로 나란히, 범례 포함
- **구좌 성과에 비교 막대** 추가 — 기존엔 글자로만 표시. 선택/비교 2줄 막대로 변경
  (막대 길이 기준을 두 기간 최댓값으로 잡아 길이 비교가 정확해짐)
- Y축 금액 포맷 개선 — `60000만` → **`6억`**
- **죽은 함수 제거** — `miniBarChart`, `renderMini`(모두 미사용)
- 차트 종류 기준 정리
  - **선그래프:** 금액 추이(매출총액·객단가·환불액·할인액·환불율·할인율)
  - **세로 막대:** 개수(주문 건수, 요일별 평균)
  - **가로 막대:** 항목 간 순위(구좌·소스·카테고리·할인종류)

### [버그] 세션(방문) 수가 부풀려짐
**배포:** `worker.js`

- **증상:** 7월 세션 331,388 (전월 대비 +100.7%). 그런데 **총 페이지뷰는 오히려 감소**
- **원인:** 세션을 `이벤트명 × 소스/매체 × 국가`로 **쪼개서 합산** → 같은 세션이 여러 번 계상
  (`totalUsers`·MAU에서 이미 겪은 것과 동일한 문제)
- **조치:** 세션을 **차원 없이 조회**한 뒤, 제외 대상(체리피커/L포인트) 조합의 세션만 따로 구해 차감
- 퍼널 '방문'도 같은 값을 쓰도록 통일

### 필터 일관성 정리
**배포:** `worker.js`

- **DAU/WAU/MAU에 L포인트 제외가 누락**돼 있던 것 수정(체리피커만 적용되던 상태)
- **유저 세그먼트에 두 필터 모두 누락**돼 있던 것 수정
  - 참여율·평균체류시간은 **평균값이라 사후 차감이 불가** → GA4 쿼리 단계에서 `notExpression`으로 제외
- 체크박스 문구: "저품질 트래픽 자동 제외" → **"저품질 트래픽 제외 (cherry picker)"**, **기본 ON**
- 필터 적용 현황
  - **적용:** 개요 KPI·퍼널, DAU/WAU/MAU, 획득, 일별추이, 구좌성과, 스크롤, UTM, 유저세그먼트
  - **의도적 미적용:** 국가별 유입표(무엇이 걸러지는지 봐야 함), 리텐션 코호트·월별 MAU(장기 추이 기준 고정), 이벤트 목록

### 응답 캐싱
**배포:** `worker.js`

- 같은 조건(기간·탭·필터·비교여부)의 응답을 **Cloudflare KV에 캐싱**
- TTL: **오늘 포함 5분 / 과거 구간만 30분** (오늘 데이터는 계속 갱신되므로 짧게)
- **성공 응답만** 캐싱(에러가 캐시에 굳지 않도록)
- 프론트 **"↻ 새로고침"** 클릭 시 `nocache=1`로 캐시 무시
- 응답 헤더에 `X-Cache: HIT/MISS` 노출(개발자도구로 확인 가능)
- **이유:** 개요 탭 1회 로드에 GA4 호출이 약 21회 발생. 캐시로 재조회 부담 제거

### 신규 지표
**배포:** `worker.js`, `index.html`

- **스크롤 도달률** (`tab=scroll`) — GA4 `scroll` 이벤트(90% 지점) ÷ 조회수.
  개별 상품 URL이 수천 개라 **페이지 유형별로 묶어서** 표시
- **UTM 캠페인 성과** (`tab=utm`) — 캠페인별/소스·매체별 유입·구매·전환율·세션당 매출
  - `utm_campaign` 없는 유입(자연·직접)은 제외
  - 매출 지표(`ecommercePurchases`/`purchaseRevenue`)가 거부되면 **세션만으로 폴백**
- **유저 세그먼트** (`tab=usersegments`) — 신규/재방문, 디바이스, 구매금액대별 행동 비교

### UI 정리
**배포:** `index.html`

- **비교 방식 드롭다운 제거** — 날짜를 직접 고를 수 있으니 불필요.
  이제 **비교 체크 + 날짜 입력**만 있고, 켜면 직전 기간이 자동 채워지고 그대로 수정 가능
  - 비교 OFF 시 날짜칸을 **숨기지 않고 비활성**(레이아웃 흔들림 방지)
- **모든 추이 그래프가 상단 기간을 따름** — 매출 추이·일별 추이의 독립 기간 버튼 제거
- **간격 규칙 통일** — 설명문 여백 8종류→1개(`.sec-desc`), 카드 하단 여백 10종류→14px,
  카드 4종(`kpi`/`funnel`/`section-block`/`mini-chart`) radius 14px·padding 18px 통일
- **제목 색·크기 통일** — 같은 계층인데 초록/회색/본문색이 섞여 있던 것 정리
- **막대바 행 높이 고정**(56px) — 54~57px로 흔들려 줄이 안 맞아 보였음
- **모바일 반응형** — 640px 이하 세로 배치, **640~1000px 사각지대** 해소(줄 밀림 수정)
- **날짜 선택 영역 컴팩트화** — 입력·버튼 높이 32px 통일, 폰트 한 단계 축소
- 퍼널 수치가 막대 밖으로 밀려 안 보이던 문제 수정
  (상품조회는 방문보다 커서 100%를 넘는데, 숫자가 막대 안 오른쪽에 있어 잘림 → **막대 밖 고정 칸**으로 이동)
- **개요 탭 최상단에 "한눈에 보기"** — 매출·방문·구매 3개 차트를 카드보다 먼저 배치
- **요일별 평균** 추가 — 이커머스는 요일 효과가 커서 프로모션 요일 결정에 활용

### 구좌 매핑 확장
**배포:** `worker.js`

- art **58개** / life **60개** 구좌
- 초기에 "탭 버튼에 href가 없어 추적 불가"로 판단했으나 **오판**.
  버튼은 화면만 바꾸지만 **그 안의 상품을 클릭하면 실제로 다른 `cate_no`로 이동**함을 확인
  - 주제별 추천작: 오리지널 866 / 판화 867 / PB Only 872 / 아트포스터 868
  - life 카테고리 칩: 테이블웨어 615 ~ 푸드 624 (연속)
- **프로모션은 `cate_no`가 캠페인마다 다름**(514, 1 등) → 숫자로 못 묶고
  **`/promotion/detail.html` 경로 패턴**으로 집계(`promoPath`)
- 검색형 작가·브랜드는 전용 카테고리 없이 검색 페이지로 이동 →
  `category_no=228`은 사이트 검색 전체 공용이라 **URL의 `keyword=` 값으로 개별 추적**
- 정적 페이지(`/company/archive.html`)는 `path` 기반 매칭 추가
- 추천작가·추천브랜드를 **실제 이름**으로 교체(아티스트1~8 → 류연희·이혜미·발롱 드 파리 등)

---

## 이전 이력 (요약)

- **GA4 속성 교체** — 511333372 → **333394141** (1년 이상 히스토리 보유, 전년 비교 가능)
- **체리피커 필터 도입** — 초기엔 국가 무관 2초 미만 → **국내 제외 + 해외 5초 미만**으로 조정
  (초기 버전은 국내 정상 트래픽까지 과도하게 걸러냈음)
- **대시보드 보안** — Worker 접근키(`DASHBOARD_KEY`), `noindex`, 클라이언트 비밀번호 게이트
- **cafe24 OAuth + KV 캐싱** — 상품·카테고리명 조회용. 토큰을 KV에 캐싱해 동시 접속 시 갱신 충돌 방지
  - 초기 실패 원인: Client ID만 헤더에 넣는 방식이 잘못됨(`invalid_grant`) → 정식 OAuth로 전환
  - GitHub API 403 → **`User-Agent` 헤더 누락**이 원인
- **리텐션 코호트** — GA4 요구사항인 `cohort` 차원 누락으로 500 에러 발생 → 추가 후 정상화
- **매출 데이터 유실 복구** — `build_revenue_daily.py`가 전체 덮어쓰기로 과거 데이터를 지우던 문제를
  **MERGE 방식**으로 수정. 백필로 2025-07-01 이후 복구(현재 394일치 보유)
- **ARPU/ARPPU → 객단가** 교체(cafe24 실데이터 기준)
- **DAU/WAU/MAU 중복 계상 수정** — 차원 없는 `totalUsers` 조회로 전환

---

## 알려진 제약 / 주의사항

1. **바로구매 경로는 추정치** — GA4 표준 리포트로 "장바구니 거친 세션 ∩ 주문서 세션"을 구할 수 없음.
   정확히 알려면 GTM으로 바로구매 버튼에 별도 이벤트 필요
2. **`begin_checkout` 이벤트 없음** — 결제 단계를 주문서 **페이지 조회**로 대체 중
3. **추천(Referral) 측정 안 됨** — 공유·바이럴 이벤트가 GA4에 연결돼 있지 않음
4. **검색형 작가·브랜드**는 검색어 기반이라, 검색어가 바뀌면 추적이 끊김
5. **할인 종류 필드명**이 cafe24 몰 설정에 따라 다를 수 있음.
   대부분이 「기타」로 잡히면 Actions 로그의 응답 키를 확인해 `DISCOUNT_FIELDS` 보정 필요
6. **프로모션·구좌 안의 개별 상품**은 수시로 바뀌므로 SLOT_MAP에 고정하지 않고 실시간 조회
7. **개발 중 Claude의 브라우저 자동화**가 GA4에 실제 트래픽으로 기록됨(2026-07-29).
   해당 일자 트래픽 분석 시 주의
